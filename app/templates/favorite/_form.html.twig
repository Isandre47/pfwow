{{ form_start(form) }}
<div class="form-group">
    {{ form_row(form.title, {'attr': {'class' : ' form-control'}}) }}
</div>
<div class="form-group">
    {{ form_row(form.link, {'attr': {'class' : ' form-control'}}) }}
</div>
    <button class="btn btn-outline-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
