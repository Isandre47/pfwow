<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow mb-5">
    <a class="navbar-brand" href="{{ path('home')}}">Accueil</a>
    <ul class="navbar-nav mr-auto">
        {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <li class="nav-link">
                <a class="nav-item" href="{{ path('app_register') }}">S'inscrire</a>
            </li>
        {% else %}
            <li class="nav-link">
                <a class="nav-item" href="{{ path('user_index') }}">Mes données</a>
            </li>
        {% endif %}
        {% if is_granted("ROLE_ADMIN") %}
            <li class="nav-link">
                <a class="nav-item" href="{{ path('admin_index') }}">Admin dashboard</a>
            </li>
        {% endif %}
    </ul>
    {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <a class="btn btn-outline-info nav-item my-2 my-sm-0 mr-3" href="{{ path('app_register') }}">S'inscrire</a>
        <a class="btn btn-outline-info nav-item my-2 my-sm-0" href="{{ path('app_login') }}">Login</a>
        {% else %}
        <a class="btn btn-outline-info nav-item my-2 my-sm-0" href="{{ path('app_logout') }}">Logout</a>
    {% endif %}
</nav>