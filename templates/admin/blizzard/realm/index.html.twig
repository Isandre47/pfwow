{% extends 'base.html.twig' %}

{% block title %}Hello RealmController!{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-10 offset-1">
            <h1>Index serveur EU</h1>
            <table class="table table-striped">
                <thead>
                <tr>
                    <td>Nom</td>
                    <td>Slug</td>
                </tr>
                </thead>
                <tbody>
                {% for realm in realms %}
                    <tr>
                        <td>{{ realm.name }}</td>
                        <td>{{ realm.slug }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
