{% extends 'base.html.twig' %}

{% block title %}Favorite{% endblock %}

{% block body %}
    <h1>Favorite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favorite.id }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ favorite.link }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ favorite.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('favorite_index') }}">back to list</a>

    <a href="{{ path('favorite_edit', {'id': favorite.id}) }}">edit</a>

    {{ include('favorite/_delete_form.html.twig') }}
{% endblock %}
