{% extends 'base.html.twig' %}

{% block title %}Favorite{% endblock %}

{% block body %}
    <h1>Favorite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Link</th>
                <td>{{ favorite.link }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ favorite.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('favorite_index') }}">retour à la liste</a>

    <a href="{{ path('favorite_edit', {'id': favorite.id}) }}">éditer</a>

    {{ include('favorite/_delete_form.html.twig') }}
{% endblock %}
