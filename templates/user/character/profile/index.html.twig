{% extends 'base.html.twig' %}

{% block title %}Hello RealmController!{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-10 offset-1">
            <h1>Index serveur EU</h1> <a href="{{ path('character_new') }}">Ajouter un personnage</a>,
            <table class="table table-striped table-dark">
                <thead>
                <tr>
                    <th>Nom</th>
                    <th>Niveau</th>
                    <th>Serveur</th>
                    <th>Ilvl</th>
                    <th>actions</th>
                </tr>
                </thead>
                <tbody>
                {% for profile in profiles %}
                    <tr>
                        <td>
                            <a href="{{ path('profile_show', {'id': profile.id}) }}">
                                <span style="color: {{ profile.characterClass.color }}">{{ profile.name }}</span>
                            </a>
                        </td>
                        <td>{{ profile.level }}</td>
                        <td>{{ profile.realm.name }}</td>
                        <td>{{ profile.equippedItemLevel }}</td>
                        <td>
                            <a href="{{ path('profile_update', {'id': profile.id}) }}">mettre à jour</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
