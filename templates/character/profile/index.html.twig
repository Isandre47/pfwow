{% extends 'base.html.twig' %}

{% block title %}Hello RealmController!{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-10 offset-1">
            <h1>Index serveur EU</h1> <a href="{{ path('character_new') }}">Ajouter un personnage</a>,
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Nom</th>
                    <th>Serveur</th>
                    <th>Ilvl</th>
                    <th>actions</th>
                </tr>
                </thead>
                <tbody>
                {% for profile in profiles %}
                    <tr>
                        <td>{{ profile.name }}</td>
                        <td>{{ profile.level }}</td>
                        <td>{{ profile.characterClass.name }}</td>
                        <td>{{ profile.realm.name }}</td>
                        <td>{{ profile.equippedItemLevel }}</td>
                        <td>
                            <a href="{{ path('profile_show', {'id': profile.id}) }}">voir</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
